import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.frontendfixup',
  appName: 'frontend-fix-up',
  webDir: 'dist'
};

export default config;
